# Ralph Progress Log
Started: lunes,  9 de febrero de 2026, 20:02:42 -03
---

## Codebase Patterns
- GSAP carousel uses `currentIndex` ref (float) as single source of truth for position
- `renderIndex` state (integer) triggers React re-renders for card content; `currentIndex` ref handles per-frame transforms
- `wrapIndex(index, length)` = `((index % length) + length) % length` for infinite looping
- Cards are absolutely positioned in overflow:hidden container; transforms computed from `centerX + (slotOffset - frac) * CARD_STEP`
- Scale range: 1.05 (center) → 0.75 (edge); Opacity range: 1.0 (center) → 0.7 (edge)
- GSAP context created on mount, `.revert()` on unmount for proper cleanup
- Direct `card.style.transform` writes are faster than `gsap.set()` for per-frame updates
- `snapToIndex(target, duration)` is the shared tween function — use it for drag release, click-to-center, and keyboard nav
- `activeTween` ref tracks current snap tween; kill it before starting a new drag or snap
- Velocity tracking: store last 5 `{x, t}` samples; compute velocity as `-dx / CARD_STEP / dt` (cards/second)
- `dragDistance` ref tracks pixel displacement for click vs drag threshold (5px)
- `touch-none` CSS class on container prevents browser touch scroll interference
- Two-tap pattern: `handleCardClick(slot)` checks `dragDistance > 5` then `logicalIndex === Math.round(currentIndex)` to decide snap vs navigate
- Keyboard nav: `handleKeyDown` on container — Arrow Left/Right +-1 via `snapToIndex`, Enter/Space navigates centered card, Escape on document level

---

## 2026-02-09 - US-001
- Replaced native-scroll carousel with GSAP-driven transform-based positioning
- Container now uses `overflow: hidden` instead of `overflow-x: scroll`
- `currentIndex` ref (float) is the single source of truth for carousel position
- 7 visible card slots (3 left + center + 3 right) with modular wrapping via `wrapIndex()`
- Cards absolutely positioned via `translateX()` with scale+opacity effects based on distance from center
- GSAP context for tween lifecycle management
- GroupCard component and ClusterPopupProps interface unchanged
- Files changed: `src/presentation/components/globe/cluster-popup.tsx`
- **Learnings for future iterations:**
  - Splitting position logic (ref-based, per-frame) from content logic (state-based, on-snap) avoids fighting React's render cycle
  - Direct style writes (`el.style.transform = ...`) outperform `gsap.set()` for high-frequency updates
  - The `gsap` import is needed even in US-001 for context setup; actual `gsap.to()` tweening will be used starting US-002
---

## 2026-02-09 - US-002
- Added momentum-based drag interaction using pointer events
- Pointer down/move/up handlers drive `currentIndex` during drag, with per-frame position updates
- Velocity computed from last 5 pointer samples; momentum snap with damping factor 0.4
- `snapToIndex()` function created as reusable tween helper — animates to target integer with power2.out ease
- `activeTween` ref ensures only one snap tween runs at a time (killed on new drag)
- `dragDistance` ref tracks pixel displacement for future click-vs-drag differentiation (US-003)
- Added `touch-none` class to prevent browser default touch gestures
- Files changed: `src/presentation/components/globe/cluster-popup.tsx`
- **Learnings for future iterations:**
  - Convert pointer deltaX to index delta via `deltaX / CARD_STEP` for 1:1 feel
  - Using a proxy object `{ val }` with `gsap.to()` is the cleanest pattern for animating a ref value
  - `setPointerCapture` ensures move/up events fire even if pointer leaves the element
  - `onPointerCancel` should mirror `onPointerUp` to handle interrupted gestures
  - `snapToIndex` updates `renderIndex` both during tween (via `onUpdate`) and on complete — the onComplete sets the exact integer to avoid float drift
---

## 2026-02-09 - US-003
- Implemented two-tap card interaction: tap non-centered card to snap to center, tap centered card to navigate
- Added `handleCardClick(slot)` callback that uses `dragDistance.current > 5` threshold to suppress clicks during drag
- Computes logical index from slot + renderIndex, compares to `Math.round(currentIndex.current)` to determine if centered
- If centered → calls `onNavigate('/groups/${groupId}')`. If not centered → calls `snapToIndex(logicalIndex, 0.5)`
- View profile button continues to work via `stopPropagation` + `pointer-events-auto` (unchanged from US-001)
- Files changed: `src/presentation/components/globe/cluster-popup.tsx`
- **Learnings for future iterations:**
  - The slot-to-logical-index mapping is `renderIndex + (slot - HALF_SLOTS)` — same formula used in `getSlotGroup`
  - `dragDistance` ref from US-002 is the key bridge between drag and click behavior
  - The 5px threshold effectively prevents accidental navigation during micro-drags
---

## 2026-02-09 - US-004
- Added keyboard navigation via `handleKeyDown` on the carousel container
- Arrow Left/Right: snap to `currentIndex ± 1` with 0.4s power2.out animation via `snapToIndex`
- Enter/Space: navigate to centered card's group (`/groups/${groupId}`)
- Escape: handled at document level (already existed from US-001)
- Container already had `role='region'`, `aria-roledescription='carousel'`, `aria-label`, and `tabIndex={0}` from US-001
- Close button already receives focus on mount from US-001
- Files changed: `src/presentation/components/globe/cluster-popup.tsx`
- **Learnings for future iterations:**
  - Reusing `snapToIndex(ci ± 1, 0.4)` for keyboard nav keeps behavior consistent with drag release
  - `Math.round(currentIndex.current)` before ±1 ensures correct integer step even if mid-tween
  - `e.preventDefault()` on arrow keys prevents page scroll when carousel is focused
---
